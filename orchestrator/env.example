# =============================================================================
# OSCR Environment Configuration
# =============================================================================
# Copy this file to .env and configure for your environment.

# -----------------------------------------------------------------------------
# Provider Selection (REQUIRED)
# -----------------------------------------------------------------------------
# Valid values: github, azure-devops
CI_PROVIDER=github

# -----------------------------------------------------------------------------
# Runner Configuration (Optional)
# -----------------------------------------------------------------------------
# Human-readable name for this runner (defaults to hostname)
# RUNNER_NAME=ci-runner-01

# Additional labels for job targeting (comma-separated)
# RUNNER_LABELS=linux,docker,self-hosted

# -----------------------------------------------------------------------------
# GitHub Actions Configuration
# Required when CI_PROVIDER=github
# -----------------------------------------------------------------------------
# Personal Access Token with repo or admin:org scope
# GITHUB_PAT=ghp_xxxxxxxxxxxxxxxxxxxx

# Repository owner (user or organization)
# GITHUB_OWNER=myorg

# Repository name (omit for organization-level runner)
# GITHUB_REPO=myrepo

# Runner group (optional, for organization runners)
# GITHUB_RUNNER_GROUP=default

# -----------------------------------------------------------------------------
# Azure DevOps Configuration
# Required when CI_PROVIDER=azure-devops
# -----------------------------------------------------------------------------
# Personal Access Token with Agent Pools (read, manage) scope
# ADO_PAT=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Organization URL (e.g., https://dev.azure.com/myorg)
# ADO_ORG_URL=https://dev.azure.com/myorg

# Agent pool name
# ADO_POOL=Default

# -----------------------------------------------------------------------------
# Advanced Configuration (Optional)
# -----------------------------------------------------------------------------
# Set to 'true' to keep runner registered on container stop (not recommended)
# RUNNER_PERSISTENT=false

# -----------------------------------------------------------------------------
# AI Agent Configuration (For odd-ai-reviewers)
# -----------------------------------------------------------------------------
# These secrets are passed to AI review workflows. Configure based on which
# agents you enable in your .ai-review.yml file.

# OpenAI API Key (required for: opencode with OpenAI, pr_agent, ai_semantic_review)
# Get your key at: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Anthropic API Key (required for: opencode with Claude models)
# Get your key at: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Azure OpenAI (alternative to OPENAI_API_KEY - all three required if using Azure)
# AZURE_OPENAI_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini

# Model selector (optional, overrides config.models.default)
# Precedence: MODEL env > .ai-review.yml models.default > hardcoded fallback
# MODEL=gpt-4o-mini

# Ollama Base URL (required for: local_llm agent)
# Default for OSCR sidecar: http://ollama-sidecar:11434
# If running Ollama on host machine: http://host.docker.internal:11434
OLLAMA_BASE_URL=http://ollama-sidecar:11434

