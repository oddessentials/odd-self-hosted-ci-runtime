# =============================================================================
# OSCR Environment Configuration
# =============================================================================
# Copy this file to .env and configure for your environment.

# -----------------------------------------------------------------------------
# Provider Selection (REQUIRED)
# -----------------------------------------------------------------------------
# Valid values: github, azure-devops
CI_PROVIDER=github

# -----------------------------------------------------------------------------
# Runner Configuration (Optional)
# -----------------------------------------------------------------------------
# Human-readable name for this runner (defaults to hostname)
# RUNNER_NAME=ci-runner-01

# Additional labels for job targeting (comma-separated)
# RUNNER_LABELS=linux,docker,self-hosted

# -----------------------------------------------------------------------------
# GitHub Actions Configuration
# Required when CI_PROVIDER=github
# -----------------------------------------------------------------------------
# Personal Access Token with repo or admin:org scope
# GITHUB_PAT=ghp_xxxxxxxxxxxxxxxxxxxx

# Repository owner (user or organization)
# GITHUB_OWNER=myorg

# Repository name (omit for organization-level runner)
# GITHUB_REPO=myrepo

# Runner group (optional, for organization runners)
# GITHUB_RUNNER_GROUP=default

# -----------------------------------------------------------------------------
# Azure DevOps Configuration
# Required when CI_PROVIDER=azure-devops
# -----------------------------------------------------------------------------
# Personal Access Token with Agent Pools (read, manage) scope
# ADO_PAT=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Organization URL (e.g., https://dev.azure.com/myorg)
# ADO_ORG_URL=https://dev.azure.com/myorg

# Agent pool name
# ADO_POOL=Default

# -----------------------------------------------------------------------------
# Advanced Configuration (Optional)
# -----------------------------------------------------------------------------
# Set to 'true' to keep runner registered on container stop (not recommended)
# RUNNER_PERSISTENT=false

# Docker network mode: bridge (default) or host
# DOCKER_NETWORK_MODE=bridge
