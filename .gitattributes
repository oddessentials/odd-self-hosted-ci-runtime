# =============================================================================
# Git Attributes for OSCR
# =============================================================================
# Enforce consistent line endings to prevent CRLF issues in shell scripts.
# Shell scripts with CRLF line endings fail with cryptic errors.

# Default: auto-detect (normalize to LF on commit, native on checkout)
* text=auto

# Shell scripts MUST use LF (Unix line endings)
*.sh text eol=lf
*.bash text eol=lf

# Dockerfiles should use LF
Dockerfile text eol=lf
Dockerfile.* text eol=lf

# YAML files should use LF (CI workflows, docker-compose)
*.yml text eol=lf
*.yaml text eol=lf

# Markdown and documentation
*.md text eol=lf

# JSON files
*.json text eol=lf

# Environment files
.env text eol=lf
.env.* text eol=lf
env.example text eol=lf

# Binary files (prevent corruption)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.tar binary
*.gz binary
*.zip binary
